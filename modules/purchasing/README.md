# purchasing

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build purchasing` to build the library.

## Running unit tests

Run `nx test purchasing` to execute the unit tests via [Jest](https://jestjs.io).
