# manufacturing

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build manufacturing` to build the library.

## Running unit tests

Run `nx test manufacturing` to execute the unit tests via [Jest](https://jestjs.io).
